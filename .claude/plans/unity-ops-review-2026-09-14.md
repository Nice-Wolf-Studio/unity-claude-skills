# Plan review — unity-ops/PLAN.md — 2026-09-14

Reviewed plan: `unity-ops/PLAN.md` (2277 lines, reference branch `main`). Reviewers: Judge (opus), Scorer (sonnet), Critic (opus). Synthesis appended below by the Synthesizer.

## Orchestrator verification notes (grounded, run by the orchestrator)

- `unity job status --help` (and `job wait`, `job cancel`, `projects exec`) printed ROOT help once in this session under the env envelope, then printed proper subcommand help on 3 immediate re-runs, with and without env vars, with flags, piped or not, exit 0. R1 observed root help; the Scorer observed proper help. Conclusion: INTERMITTENT. Rule that survives: never treat root-help output as evidence of absence; re-run or probe the parent's `Commands:` list. The plan's categorical "falls through" wording is wrong.
- `unity pipeline list --help` → only `-h`; `--project-path` → `error: unknown option '--project-path'`. `pipeline list --format json` → `data.instances[]`, `data.latestVersion`, `data.summary.{totalInstances,runningInstances,instancesWithPipeline,reachableServers,instancesInSafeMode,instancesWithUpdateAvailable}`. Machine-wide; no per-project flag.
- `unity status --format json` with no Editor → `success:false`, `data.count:0`, `data.instances:[]`, `errors[0].code:"STATUS_NO_INSTANCES"` (Judge reports exit 6). DESIGN.md §4 row 4 keys on "exit 6 / AMBIGUOUS_EDITOR" and therefore captures the no-instances case before row 5 (sandbox ASK). Confirmed ordering defect.
- Installed skill `~/.claude/skills/unity-cli/SKILL.md` is 441 lines; line 439: "latest: `1.0.0-beta.8`". references/: auth-license-cloud, build-run-test, collaboration, config-hub, diagnostics-maintenance, editors-install, integration-advanced (516 lines), projects-templates. NO version-control.md. R1 anchored to GitHub@main (beta.9 docs). `grep --affected|--list-modules|plugin changelog` in the installed skill → 0 hits.
- Installed SKILL.md:167 "Before editing any scene, GameObject, prefab, or asset, run `unity status`…"; :179 "Never hand-edit `.unity`, `.prefab`, or `.asset` YAML while a live Editor is reachable"; :184 the disclosure sentence "no live Editor detected, editing the file directly"; :409 `8) echo "Tests failed — report to developers, do not retry"`. The preflight rules and the exit-8 recipe are already in the dependency's body text.
- `~/Dev/Unity/ai_test`: git repo on `main`, 33 dirty entries (M Assets/Scenes/SampleScene.unity, 20× M Logs/*.log, 12 untracked .meta/.json). 0 `.asmdef`, 0 `[Test]`/`[UnityTest]` files. No Editor running (`STATUS_NO_INSTANCES`). `Library/PackageCache` has no com.unity.pipeline (not installed yet).
- Build module: `/Applications/Unity/Hub/Editor/6000.3.10f1/PlaybackEngines` does not exist at that path; Critic found `MacStandaloneSupport` under `Unity.app/Contents/PlaybackEngines`.
- Marketplace repo `~/Documents/GitHub/wolf-skills-marketplace`: checked-out branch `feat/note-upload-agents` (old flat layout, 35 root `unity-*` dirs). `origin/main` (c984292): per-plugin layout — top-level `unity/`, `unity/skills/` holds 35 skills; `marketplace.json` has 16 plugins, every block is exactly {name, description, source, strict} with `source: "./unity"` — NO `skills` array; CHANGELOG top is `## [3.4.0] - 2026-08-29`. Local `main` (8f713d8) is NOT an ancestor of `origin/main` → `git pull --ff-only` fails. CI on origin/main: `.github/workflows/skill-validation.yml`, `scripts/skill-validator.ts`, `evals/collision-lint.sh`, `evals/description-lint.sh`, `evals/hook-injection.test.sh`, `evals/run-trigger-evals.ts`, `evals/router-precedence/scenarios.json`. The installed marketplace copy (`~/.claude/plugins/marketplaces/wolf-skills-marketplace/`) matches origin/main's layout. R3 read the stale branch.
- Issues #4–#8 (unity-claude-skills) and #57 (marketplace) exist as `wolfagents-bot`; #57's body describes the stale flat layout and needs correction.

---

## Judge (raw output)

PROBLEM_UNDERSTANDING: Weak
PREMISE_VALIDITY: Questionable
VERDICT: FAIL
VERDICT_REASON: The failure modes are real and the plan's rigor is high, but its factual base was derived from a copy of the dependency that is not installed — producing a falsifiable-and-false "restates nothing" premise, a flagship Safe-Mode command that exits 2, a decision table that routes its own flagship scenario to the wrong branch, a version-drift skill whose premise doesn't exist locally, and an advisory→hard-gate architecture with no enforcement mechanism — so the research must be re-derived against the installed skill and the enforcement question answered before this is worth executing.

ESSENTIAL_FINDINGS:
1. Research derived from GitHub@main (beta.9 skill), not the installed embedded skill (beta.8, 441 lines, no version-control.md). R1's anchors `SKILL.md:443`, `integration-advanced.md:546-582`, `version-control.md` do not resolve locally. Every "Unity already covers it" cut (DESIGN.md:42-54) and every "gap" claim is unverified against the runtime dependency.
2. `unity-cli-contract`'s drift premise does not exist locally: installed skill documents beta.8; grep for `--affected|--list-modules|plugin changelog` → 0 hits. The four-row "documented but absent" table has no local source; the RED scenario ("the docs are right there in the unity-cli skill") cannot occur. The version-probe rule survives; the narrative, table, scenario and baseline do not.
3. `unity pipeline list --project-path …` → `error: unknown option`, exit 2. Used 19× in PLAN.md, 15× in DESIGN.md as the per-project Safe Mode detector; the command is machine-wide and the plan never specifies filtering.
4. Decision table routes the flagship scenario wrong: `unity status` with no instances → `success:false`, `STATUS_NO_INSTANCES`, exit 6. Row 4 (exit 6 / AMBIGUOUS_EDITOR → LIVE with --project-path) fires before row 5 (sandbox ASK). `STATUS_NO_INSTANCES` appears nowhere in DESIGN/PLAN/R1.
5. Advisory→hard-gate has no mechanism: markdown cannot refuse/block/inject; G11 forbids hooks/; PreToolUse/PostToolUse never considered (DESIGN.md:454-462 only rejects a SessionStart informational hook). "Promotion is a one-word edit" is false. R3 checklist 1 permits hooks; R3 §5:169 gives the shape.
6. "Nothing restated" is false: installed SKILL.md:167-188 already has the preflight rules, the never-hand-edit block, the disclosure sentence, the Safe Mode pointer; SKILL.md:399-414 has the exit-code recipe (8 = do not retry) and TESTS_FAILED/TEST_RUN_ERROR/TEST_TIMED_OUT. DESIGN.md:478 says restating is a defect, then restates at :261/:270/:281-282/:327/:309/:326/:86/:359/:76/:96/:169/:179/:188/:356/:75/:79/:95/:360/:271/:272/:215/:225/:226/:238. Restatement share per skill: live-edit ~10% (additive; no "read-back"/undo in the installed skill), script-change ~45%, destructive ~50%, preflight ~70%, cli-contract ~65%, batch-hygiene ~75%.
7. GREEN gate cannot pass: ai_test has 33 dirty files incl. SampleScene.unity (5,977-insertion diff). PLAN.md:557-563 "Revert it — git checkout -- <file>" would destroy pre-existing work. Affects Tasks 1.2, 1.4, 3.2, 6.2, 6.4.
8. DX metrics have no collection mechanism (defined as "counted from session transcripts"; no task builds collection; CI is a non-goal; hooks banned). The 30-day keep/promote/cut decision has no input.

NON_ESSENTIAL_FINDINGS:
- Test protocol never tests triggering (every result run force-feeds the skill: "Read it before acting"), yet non-triggering is rated H/H. G9's four banned phrases are thin vs the installed description's "or run any other Unity CLI operation".
- N=1 self-graded baselines; no repetition; the scenario states the predicted rationalization and the author judges.
- Four of six descriptions start "Use after/before" (PLAN.md:1077/1271/1472/1640), violating G9; conformance check 2 prints only `description: >` on a folded scalar — never inspects the value; PLAN.md:96 vs :755 contradictory.
- Task 5.3 calibrates against a project with zero tests (0 asmdef, 0 [Test]); build floor uncalibrated if module missing.
- LIVE scenarios have no Editor bootstrap; `unity open` appears nowhere; the mitigation is the check the plugin declares unreliable.
- R1's `close` citation `SKILL.md:240` is wrong; the installed skill documents "exits without saving" nowhere → destructive-gate is MORE additive than claimed. R1's `projects clean` row overstates: `projects-templates.md:332` says it refuses while an editor holds the project; `--force` is for the indeterminate case.
- wolf-core's real chaining mechanism is `dependencies:` frontmatter; G8 bans it; `## Chain` is prose.
- The five CONFUSES defects in the loaded plugin are actively wrong instructions; deferring them while adding six skills above a mis-audited dependency is questionable ordering (Jeremy decided: issues, separately — not reopened).
- Marketplace arithmetic and G1 verified as the plan states (NOTE: superseded by the Critic/orchestrator finding that origin/main has a different layout).

---

## Scorer (raw output)

COMPOSITE: 8.1/10 — Completeness 10, Failure Coverage 6.7, Backward Chain 6, Feasibility 8, Integration 10, Testability 8.
WEAKEST_DIMENSION: Backward Chain (6/10) — RED/GREEN evidence chain for increments 1, 3, 6 depends on a bare `git status --short` in ai_test being clean; it is already dirty on the exact file the flagship scenario targets, and no task snapshots or cleans first.
VERDICT: CONDITIONAL_PASS
VERDICT_REASON: No ≤3 veto; exceptionally well-evidenced; but one falsified technical claim baked into unity-cli-contract three times ("`unity job status --help` falls through to root help" — live-tested false; ORCHESTRATOR NOTE: intermittent, see above) and one uncovered edge case (pre-existing dirty ai_test tree) must be fixed before execution.
Other NO items: "exactly six tasks per increment" contradicted by Increment 5 having seven (5.1–5.7); pre-mortem lacks a row for a dirty testbed.
Verified YES items of note: G1 (bare `unity` → command not found; sourced → 1.0.0-beta.8); `unity test --affected` absent; all 10 command names present in installed integration-advanced.md; `pipeline install/list/list-versions` flags exist; plugin.json six keys; issues #4–#8 and #57 exist with matching titles; ~/.claude.json has no unity-ops key yet; CHANGELOG top `[2.14.0]` on the CHECKED-OUT branch (superseded: origin/main top is `[3.4.0]`).

---

## Critic (raw output)

VERDICT: FAIL
VERDICT_REASON: Increment 8 is written against a marketplace layout that no longer exists on origin/main (per-plugin dirs, 16 plugins, [3.3.0]/[3.4.0] CHANGELOG, no `skills` array, `git pull --ff-only` aborts); the testbed cannot satisfy Increment 1's GREEN gate (33 dirty files incl. SampleScene.unity); no Editor is running and no authorized task opens one for the four LIVE increments; and the two flagship RED baselines are predicted against behavior Unity's always-installed skill already mandates in its body text — which by the plan's own halt rule stops the increment that blocks four others.

FATAL_FLAWS (all ESSENTIAL):
1. Task 8.2 unexecutable: `git pull --ff-only origin main` fails (main 8f713d8 vs origin/main diverged); origin/main has 0 root `unity-*` dirs (per-plugin layout, `unity/skills/`); `unity` block is {name, description, source:"./unity", strict} with no `skills` array; 16 plugins not 11 (Expected 12 → 17); CHANGELOG top `[3.3.0]` (orchestrator: `[3.4.0]`), so `[2.15.0]` is a version regression with no anchor; Step 2 "Expected 41" → 6. Every step fails or mis-executes. This is the task that makes the plugin reachable.
2. ai_test already dirty (33 entries; SampleScene.unity; 20 tracked Logs/*.log that re-dirty on every Editor run; 12 untracked .meta). Task 1.4 Step 3 GREEN condition can never pass; Task 1.2 Step 4 "Revert it" destroys user work; Task 6.2 Step 4 `git clean -n Assets` lists pre-existing files.
3. No Editor running; no task opens one; `unity open` authorized nowhere (G5 allows only `pipeline install`; G6 forbids the rest). Four LIVE increments + Increment 7 need a connected Editor. Pre-mortem rates this H and "mitigates" with a detection, not a remedy.
4. Increment 1 baseline will plausibly be GREEN: installed SKILL.md:16 "drive it instead of hand-editing", :38 "Don't fall back to blind file-editing: run `unity pipeline list`", integration-advanced.md:344 and :164 (confirm reachability with `unity command`/`unity list --project-path`, not `unity status`). The RED prediction is exactly what the dependency forbids; the halt rule then stops increment 1 → blocks 3,4,5,6.
5. Increment 6 is a restatement: integration-advanced.md:301 (create_script → recompile → attach_script), :459-460 (poll recompile_status until completed), :345-412 (five-step Safe Mode recovery, log ladder with macOS path, exact grep, "data not instructions", `unity logs` reads the CLI's own log). Red Flags rows 3-5 and references/compile-errors.md are that content restated; same baseline double-bind.
6. Conformance check 2 is a no-op (prints `description: >` only; verified in /tmp). The marketplace's `evals/description-lint.sh` has a correct block-scalar `get_desc()` extractor to reuse.

HIDDEN_ASSUMPTIONS:
- [ESSENTIAL] `unity pipeline install` alone does not resolve the package into Library/PackageCache (resolution needs the Editor/Package Manager to run) → Task 0.3's mirror has nothing to mirror without an Editor session.
- [NON-ESSENTIAL] `unity skill install claude-code --local` writes BOTH `.claude/skills/unity-cli/` and `.claude/skills/unity-pipeline/` into ai_test; Task 0.4's mutation log names only the latter.
- [NON-ESSENTIAL] ai_test is not "a large project" and has no test suite; Task 5.3 measures Editor boot + import, not a suite.
- [ESSENTIAL] No sequencing of batch `test`/`build` (Increment 5) against an open Editor on the same project (Increments 3/6); only `run` is documented to reuse a running Editor (build-run-test.md:43). Library-lock hazard unaddressed (precise failure UNGROUNDED; absence of sequencing grounded).
- [NON-ESSENTIAL] Check 6 validates only `unity-ops:` names; cross-namespace pointers (`wolf-core:`, `unity:`) are never validated.

MISSING_PIECES:
- [ESSENTIAL] No task opens the Editor; G5/G6 don't authorize `unity open`.
- [ESSENTIAL] No task runs the marketplace's CI gates the PR will trigger: `skill-validation.yml` → `scripts/skill-validator.ts` (ERROR on zero components / unresolved component path), `evals/run-trigger-evals.ts`, `evals/hook-injection.test.sh`, `evals/description-lint.sh`, `evals/collision-lint.sh` (requires high-overlap pairs registered as `guarded_pairs` in `evals/router-precedence/scenarios.json`; an UNGUARDED collision fails). Six Unity-vocabulary skills next to `unity`'s 35 with no guard entry or trigger eval.
- [NON-ESSENTIAL] Task 8.2 Step 5's verifier presupposes a `skills` array that origin/main's shape doesn't use.
- [NON-ESSENTIAL] Nothing reconciles two plugin roots in one repo (root `.claude-plugin/plugin.json` = `unity` v1.4.0 over `skills/`; new `unity-ops/.claude-plugin/plugin.json`) — how a marketplace entry addresses the nested one is unstated; root cause of the hand-mirroring.

WILL_BREAK_WHEN (selected): Task 8.2 Steps 1–4 (as above); Task 1.4 Step 3 (33 dirty lines regardless); Task 1.2 Step 4 checkout destroys SampleScene.unity work; any LIVE scenario today (no Editor); Tasks 1.2/6.2 baselines GREEN → halt; Task 2.4 Step 3 `grep -c 'affected'` counts lines — a correct transcript has ≥4 → false negative; check 4 prints two lines (`0` then `0 forbidden tokens`) because `grep -c` prints 0 and exits 1; heading-count Expecteds off by one in Tasks 1.3/2.3/5.4 (H1 not counted by `^## `); Task 8.2 Step 2's "35-name list in audit §5" cites a bucket count, not a directory list; Task 0.1 Step 2 "row ends in `installed`" also matches `not installed`; Task 1.3 Evidence says `instancesInSafeMode: 0` but the path is `data.summary.instancesInSafeMode`; `data.instances[].safeMode.detected` (integration-advanced.md:359) never observed.

HOLDS UP (verified by Critic): `--detach` exists on `unity command`; `unity job` lists status/wait/cancel; MacStandaloneSupport present under `Unity.app/Contents/PlaybackEngines`; `--no-tail`/`--output-path`/`--timeout` exist on `build`; no root `version` command; `skill install --local` mirrors `unity-pipeline` (integration-advanced.md:108); `unity close` help says "(exits without saving)"; `~/.claude.json` keys are `name@wolf-skills-marketplace`; `pipeline list-versions` latest 0.7.0-exp.1; gh active as wolfagents-bot; issues #4–#8 exist; ai_test is a git repo at 6000.3.10f1 with Library/ ignored; DESIGN §4 has 10 rows; six names collide with none of the 35; BSD grep handles `\|`; description-lint would pass six "Use when …" descriptions; checks 1, 3, 5, 6 work as specified.

---

## Synthesizer (raw output)

VERDICT: REJECTED

AGREEMENTS (high confidence):
- [J1 + C + orch] Research base is the wrong artifact: R1 anchored to `Unity-Technologies/skills@main` (beta.9 docs); the installed dependency is `~/.claude/skills/unity-cli` (441 lines, beta.8, 8 reference files, no `version-control.md`). R1 anchors `SKILL.md:443`, `integration-advanced.md:546-582`, `version-control.md` do not resolve. Every DESIGN.md:42-54 "already covered" cut and every §G gap is unverified.
- [J3 + orch] `unity pipeline list --project-path` → unknown option, exit 2; machine-wide; used 19× PLAN / 15× DESIGN as the per-project Safe-Mode detector with no filtering specified.
- [J4 + orch] `unity status` with no Editor → `success:false`, `STATUS_NO_INSTANCES`, exit 6; DESIGN §4 row 4 (exit 6 / AMBIGUOUS_EDITOR → LIVE) swallows the flagship case before row 5 (sandbox ASK).
- [J6 + C5 + orch] "Nothing restated" is false: installed SKILL.md:167/:179/:184/:409. Restatement share: preflight ~70%, cli-contract ~65%, batch-hygiene ~75%, destructive ~50%, script-change ~45%, live-edit ~10%.
- [J7 + C2 + S] GREEN gate unsatisfiable: ai_test 33 dirty entries incl. SampleScene.unity; "Revert it — git checkout --" destroys user work. Affects T1.2/1.4/3.2/6.2/6.4.
- [J5 + J8] Advisory→hard-gate has no mechanism; DX metrics have no collector; "promotion is a one-word edit" is false.
- [C1 + orch + synth re-verified] Increment 8 targets a layout that doesn't exist on origin/main: 16 plugins, blocks are {name, description, source:"./unity", strict}, no `skills` array, per-plugin dirs (`unity/skills/`, 35), 0 root `unity-*`, CHANGELOG top `[3.4.0] - 2026-08-29`; local main not an ancestor of origin/main → ff-only aborts.
- [C6] Conformance check 2 is a no-op on folded scalars.
- [C-missing] No task runs the marketplace CI gates (skill-validation.yml → skill-validator.ts, description-lint, collision-lint with guarded_pairs, run-trigger-evals, hook-injection.test).
- [C3] No Editor running; no task opens one; `unity open` unauthorized. Four LIVE increments + Increment 7 need one.
- [C4 + C5] Both flagship RED baselines predicted against behaviour the installed skill already mandates; halt rule then blocks 3,4,5,6.
- [C-hidden] `pipeline install` alone doesn't resolve the package into Library/PackageCache; T0.3 has nothing to mirror without an Editor run.
- Four descriptions start "Use after/before" (G9 violation); PLAN.md:96 vs :755 contradict.
- T5.3 calibrates against a project with 0 asmdef / 0 tests.

DISAGREEMENTS:
- Nested `--help` fallthrough — RESOLVED by orchestrator: INTERMITTENT. Both "always" (R1/plan) and "false" (Scorer) are wrong. Surviving rule: root help is not evidence of absence; re-probe via the parent's Commands list.
- Marketplace layout — Judge/Scorer "arithmetic checks out" UNSUPPORTED (run on the stale checked-out branch); Critic + orchestrator + synthesizer (origin/main) stand.
- Verdict — Scorer CONDITIONAL_PASS is UNSUPPORTED by scope (did not test the installed-vs-GitHub dependency, decision ordering, pipeline list scoping, enforcement mechanism, origin/main). Two grounded FAILs decide.
- `unity close` — not a conflict: the CLI help says "exits without saving"; the installed SKILL does not → destructive gate is MORE additive; R1's `SKILL.md:240` anchor still wrong.
- `projects clean` — OPEN QUESTION: Judge (grounded, uncontested) says projects-templates.md:332 refuses while an editor holds the project; `--force` is the indeterminate case. Re-derive before the guardrail text ships.
- Increment 5 has seven tasks vs "exactly six" — wording fix.

BACKWARD_CHAIN: BROKEN at steps 1, 2, 4, 5, 6, 7 (only step 8 — Jeremy enabling the plugin — and step 3 — blocked by 1 — are not independently broken). The chain never leaves the bootstrap.

SCORE_SUMMARY:
| Dimension | Scorer | Synthesizer note |
|---|---|---|
| Completeness | 10 | Stands |
| Failure Coverage | 6.7 | Stands, generous |
| Backward Chain | 6 | Overstated; six broken links → ≤4 |
| Feasibility | 8 | Overstated (stale branch, no-Editor, unresolved package) → ≤5 |
| Integration | 10 | Overstated (wrong target shape) → ≤4 |
| Testability | 8 | Stands; checks 2 and 4 are no-ops; triggering never tested |
| COMPOSITE | 8.1 | Re-derived ≈6.4; no ≤3 veto; REJECT comes from two grounded FAILs |

FIX_LIST:
### Bucket A — Research
- A1 [MUST] Re-derive R1 §B/§G against the INSTALLED skill; re-anchor every citation; re-validate or withdraw every "already covered" cut and every gap.
- A2 [MUST] Restatement audit per skill (lines duplicated vs additive margin). Feeds B4, C10.
- A3 [MUST] Re-derive the version-drift premise: installed skill has 0 hits for --affected/--list-modules/plugin changelog; the evidence is the GitHub@main-vs-installed diff.
- A4 [MUST] Re-derive `pipeline list` contract (no --project-path; machine-wide; keys) and how a per-project answer is derived.
- A5 [MUST] Re-derive the no-Editor envelope (STATUS_NO_INSTANCES, exit 6); confirm whether `data.instances[].safeMode.detected` is ever emitted.
- A6 [SHOULD] Re-check `close` anchor (CLI help, not the skill) and `projects clean` (refuses while an editor holds the project; --force for indeterminate).
- A7 [SHOULD] Record nested --help as intermittent; deliverable is the probe rule.
- A8 [SHOULD] Build module path: MacStandaloneSupport under Unity.app/Contents/PlaybackEngines.
### Bucket B — Design
- B1 [MUST] §4: add STATUS_NO_INSTANCES predicate evaluated BEFORE the exit-6/AMBIGUOUS_EDITOR row; row 4 also requires non-empty data.candidates.
- B2 [MUST] `pipeline list` is machine-wide; specify the filter on data.instances[] by resolved project path; no --project-path on any pipeline list line (15 occurrences).
- B3 [MUST] Enforcement mechanism: fail-open PreToolUse hook shipped in shadow/log-only mode in v1, JSONL decision records = DX-metric collector; promotion = flip a pattern's decision from log to deny; wolf-core hook shape (R3 §5); amend G11 to permit hooks/.
- B4 [MUST] Thesis → additive-margin claim; restatement budget ≤25% of body lines, each restating line must carry the enforcement it adds; skills that can't clear it are cut or merged.
- B5 [MUST] cli-contract premise: "the skill can run ahead of or behind the binary — probe first"; the four-row table labelled upstream-documented/unverified locally/non-authoritative.
- B6 [MUST] Editor bootstrap + testbed protocol (who opens the Editor, command, authorization, precondition assertion per LIVE scenario).
- B7 [SHOULD] Resolve two plugin roots in one repo; state how a marketplace entry addresses the nested root.
- B8 [SHOULD] Each DX metric names its collector (B3 JSONL) or a manual protocol with an owner.
### Bucket C — Plan
- C1 [MUST] Task 0.0 testbed snapshot (git status --porcelain + git stash create manifest); gates become "no entries absent from the snapshot"; revert only paths not in the snapshot. Affects T1.2 S4, T1.4 S3, T3.2 S4, T6.2 S4, T6.4 S4.
- C2 [MUST] Amend G5/G6 + Task 0.2a: authorize `unity open` in ai_test only; documented precondition for every LIVE scenario.
- C3 [MUST] Task 0.3: open the Editor after pipeline install and before the --local mirror; verify Library/PackageCache entry exists first.
- C4 [MUST] Task 0.5: minimal EditMode test assembly in ai_test (one asmdef + one [Test]) so T5.3 measures a suite.
- C5 [MUST] Task 0.1 S2: `installed` substring also matches `not installed`; anchor the grep.
- C6 [SHOULD] Task 0.4 mutation log: --local writes both unity-cli and unity-pipeline into ai_test.
- C7 [MUST] 3 baseline reps per skill; predicted rationalization removed from the runner's brief; RED holds on a majority.
- C8 [MUST] Natural-trigger result run in addition to the force-fed one.
- C9 [MUST] Baseline Control states: 3/3 baselines comply → the skill is CUT — a valid TDD outcome, not a plan failure.
- C10 [MUST] Re-target Increment 1 and 6 scenarios at pressure the dependency's text does not already defeat, or narrow the skills to their additive margin.
- C11 [MUST] Task 2.1 / :783 / :865 / G3: delete the categorical root-help environment line and secondary failure; keep fallthrough only as a probe rule.
- C12 [MUST] Check 2: reuse evals/description-lint.sh get_desc(); assert the VALUE begins "Use when".
- C13 [MUST] Check 4: grep -Eiq … && echo FORBIDDEN || echo "0 forbidden tokens".
- C14 [MUST] Heading-count Expecteds off by one (T1.3 S3, T2.3 S5, T5.4 S3).
- C15 [MUST] All six descriptions begin "Use when"; resolve :96 vs :755 in favour of :96.
- C16 [MUST] Task 2.4 S3 / 2.2 S3: count distinct flag spellings, not lines.
- C17 [MUST] Safe-Mode key path is data.summary.instancesInSafeMode; mark safeMode.detected as upstream-documented, never observed.
- C18 [SHOULD] Check 6: validate cross-namespace pointers.
- C19 [SHOULD] Increment 5: sequence batch runs vs an open Editor; fix "exactly six tasks".
- C20 [MUST] T8.2 S1: `git fetch origin && git checkout -b feat/register-unity-ops origin/main`; delete the stale-branch note.
- C21 [MUST] T8.2 S2: mirror into per-plugin root `wolf-skills-marketplace/unity-ops/{.claude-plugin/plugin.json, skills/<six>}`; delete the 41 assertion.
- C22 [MUST] T8.2 S3: block is exactly {name, description, source:"./unity-ops", strict:false}; count 16 → 17.
- C23 [MUST] T8.2 S4: CHANGELOG entry above [3.4.0] → [3.5.0]; update S5 Expected and the File Structure row.
- C24 [MUST] T8.2 S5: rewrite the verifier (no skills array).
- C25 [MUST] New T8.2 S5a: run skill-validator.ts, description-lint.sh, collision-lint.sh (register guarded_pairs in evals/router-precedence/scenarios.json), run-trigger-evals.ts, hook-injection.test.sh locally before the PR.
- C26 [SHOULD] T8.2 S2 collision check → `git ls-tree origin/main unity/skills/`.
- C27 [MUST] Correct wolf-skills-marketplace#57's stale layout evidence.

TL;DR: REJECTED — the plan is unusually rigorous but was researched against the wrong copy of its dependency and the wrong branch of its publish target, so its two flagship RED baselines will likely come back GREEN and halt the increments that block four others, while Increment 8 fails at every step against origin/main; fix Buckets A and B before touching a line of Bucket C.

---
---

# ROUND 2 — re-review of revision 2 (2026-09-14)

Revision 2: DESIGN.md 848 lines, PLAN.md 3516 lines / 57 tasks, all six skills narrowed to ≤25% restatement, shadow PreToolUse hook as Increment 1, Increment 9 rebuilt against origin/main. Orchestrator patched two Scorer defects immediately (cli-contract heading count 5→4; `--affected` grep form).

## Judge (round 2) — PASS_WITH_CONCERNS
PRIOR_FIXES: A1 PARTIAL (two false absence claims survive: `vcs uvcs locks|changesets|review` IS documented at SK:257,263,280 and PT:175-220 — only the new verb family and `vcs doctor` are absent; `unity plugin install plastic` IS documented at SK:286, PT:259 — only `plugin changelog` is absent; four anchors off: SK:26→SK:32, SK:17→SK:15, SK:16/38→SK:36). A2 RESOLVED. A3 RESOLVED. A4 RESOLVED (research layer never records the flag's absence). A5 RESOLVED. A6 RESOLVED (PT:332 refuses while an editor holds the project; `--force` binary-only). A7 PARTIAL (research files never say "intermittent"; R1:31 still categorical while PLAN:20 calls R1 authoritative for binary behaviour). A8 RESOLVED. B1 PARTIAL (see E4). B2 RESOLVED. B3 PARTIAL (E1–E3). B4 RESOLVED. B5 PARTIAL (vcs/plugin rows wrong). B6 RESOLVED. B7 RESOLVED. B8 RESOLVED (contingent on B3).
ESSENTIAL: E1 scenario-tagging has no delivery mechanism (Agent tool has no env; hook process is spawned by the harness; T1.3 S4 invokes the script directly so it proves nothing about dispatch). E2 hook walks up from `$PWD`, never reads stdin `cwd`, never walks up from `file_path`; every call from the plugin repo exits at guard 1 → serialized-asset-write and cs-write take false negatives. E3 `destructive-cli` matches bare `--yes|--force` on any command (`git push --force`, `npm i --force`), and `self-(update|uninstall)` unanchored; that pattern is pre-committed to `deny`. E4 §4 row 3a "GO TO ROW 5" skips row 3b (the `unity list --project-path` headless check) though row 5's predicate requires 3b and IA:162-164 says a headless Editor produces exactly STATUS_NO_INSTANCES; contradicts §3.1 evidence row and the RED GREEN-criterion. E5 two false absence claims (vcs, plugin) in the drift-authority table, propagating to DESIGN:88 and §6:739. E6 R5's load-bearing rows ("hooks fire in subagents", "additionalContext reaches the model") are unexercised; only bare URLs.
NON-ESSENTIAL: G19/T0.4 say `pipeline list --help` shows only `-h` (a Global Options block also prints); "eleven-row" table has twelve rows (PLAN:1611); "seven gates" = six + npm install; CHANGELOG date placeholder never pinned/asserted; preflight budget at exactly 25% with the check asserting only the numerator; hook `case`/`elif` makes patterns mutually exclusive (`unity test --allow-install` → destructive-cli only); `cs-write` never fires for `create_script`; R1b `BRT:414` dangling (file is 349 lines); README:145→147; leftover self-correction text at PLAN:1632; Task 3.1 LIVE baseline could run an unbounded `unity test`.

## Scorer (round 2) — CONDITIONAL_PASS, composite 9.3
C1–C26 all RESOLVED except C14 PARTIAL (cli-contract heading count 4 vs Expected 5 — PATCHED by orchestrator). Verified live: 33 dirty entries; `git stash create` non-destructive; `pipeline list` six summary keys, exit 0; `status` exit 6 STATUS_NO_INSTANCES; check 2 `get_desc()` byte-identical to `evals/description-lint.sh:26-37`; checks 1–6 produce their Expected lines on a dummy; T9.3 invocations byte-identical to `skill-validation.yml:30-49`; 16 plugins / 4-key block / `[3.4.0]`; `banker/hooks/hooks.json` shape matches. Defect: Task 3.1 "`grep -r affected` returns nothing" is false (matches "unaffected") — PATCHED to the `--affected` flag form.

## Critic (round 2) — FAIL
PRIOR_FLAWS_STATUS: all prior FATAL/MISSING items CLOSED or PARTIALLY CLOSED on paper; Increment 9 verified end-to-end against origin/main (plugins 16→17, 4-key blocks, `unity/`=2 children, `unity/skills/`=35, 102 SKILL.md → "108" correct, T9.3 byte-identical to the workflow). Synth B4 NOT CLOSED as a check (`grep -c 'restates '` counts author-written markers). C7/C8/C9 present but unfalsifiable (F3, F5 below).
FATAL (all ESSENTIAL, all executed):
F1 Hook guard keys on `$PWD` (PLAN:1021), never `.cwd` from stdin, never `dirname(file_path)`; `~/.claude/settings.json` sets `CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=1`; every task runs from the plugin repo (no ProjectVersion.txt above it) → guard 1 exits 0 on every call → zero records, zero advisories; T1.3's four assertions `cd ~/Dev/Unity/ai_test` and pipe stdin by hand, proving nothing about registration-time behaviour.
F2 `UNITY_OPS_SCENARIO` has no implementation (Agent tool has no env parameter; `export` doesn't survive a Bash call or a dispatch); G18's rule then voids all 12 baseline reps and 12 result runs.
F3 RED gate self-satisfies: `grep -c 'Predicted rationalization present?  *YES'` returns 3 against three unmodified copies of the recording template line (PLAN:1446 has `YES/NO — quote it`); "RED holds when YES ≥2" can never fail → CUT unreachable. Trigger gate self-satisfies: `grep -ci 'surface-preflight'` (PLAN:1676) matches the Step-3 brief recorded in the same file (PLAN:1662) → TRIGGER-FAIL unreachable.
F4 Testbed gate passes on (a) overwriting a pre-existing untracked file, (b) deleting one (gate computes only now−before), (c) creating a file inside a pre-existing untracked dir (porcelain shows one `?? sub/` line). ai_test has 13 untracked entries, none hashed (T0.0 S2's awk skips `??`); T0.5/T0.6 add `?? .claude/` and `?? Assets/Tests/` to the snapshot → those subtrees become gate-blind, and `.claude/skills/unity-pipeline/` is what increments 4 and 7 read. `git stash create` captures tracked changes only (verified: 2-parent commit, untracked absent) so PLAN:289/:342's recovery claim is false; the object is unreferenced and GC-able.
F5 Natural-trigger runs don't test triggering: the subagent gets a prose list; no unity-ops skill is registered until T9.5 (`--plugin-dir`), after all 12 result runs are committed; the competitor `unity-cli` is mis-stated (one line vs its 100-word catch-all description).
F6 `destructive-cli` false-positives: `git push --force`, `git checkout --force`, `npm install --force`, `brew upgrade --force`, `apt-get install --yes`, `docker compose down --force`, `yarn install --force`, `cargo install --force`, `pip install --yes`, `./configure --force`, and `grep -n 'unity close' unity-ops/PLAN.md` / `git commit -m "…unity close…"` all MATCH; metric 4's tripwire (`unity[ ]+close`) fires on a grep of the plan's own text → immediate promotion to `deny` → `git push --force` blocked in any Unity project dir.
HIDDEN (ESSENTIAL): H1 the hook is never registered during increments 1–8 (no `--plugin-dir`/install before T9.5) → decisions.jsonl holds only the four hand-piped T1.3 records, which is why T1.4's "≥4 lines" passes. H2 `additionalContext` on PreToolUse cited only to the general guide. H3 if T0.4 finds no project-path field on `data.instances[]`, row 1 (evaluated first) fires on any machine-wide Safe-Mode Editor and routes to "fix compile errors" for a project not in Safe Mode. NON-ESSENTIAL: exec bit `test -x` in hooks.json vs `test -f` in DESIGN; T0.5 "exactly two dirs" rests on IA:108 alone.
MISSING: M1 CUT breaks three Increment-9 Expecteds (T9.2 S6 hard-codes 6; T9.4 S2 hard-codes 25 files; CHANGELOG names all six). M2 Increment 3's CUT row gives no instruction for dependents; cli-contract carries the only day-one hard gate. M3 T0.0's `"stash_object": "<paste…>"` placeholder never verified replaced; object unreferenced/GC-able. M4 T9.5 S1 has no Expected, starts an interactive session, and its hook check requires a real `.unity` write under ai_test. M5 restatement budget has no independent measurement.
WILL_BREAK: hook enabled with session cwd ≠ Unity root → nothing logged; `grep -n 'unity close' PLAN.md` from a Unity dir → promotion tripwire; T9.2 S2 `grep -c … || echo` prints two lines (C13 class reintroduced); T6.2 S3 `grep -ci 'timeout'` signal inverted (an agent that explicitly declines a timeout reads as GREEN); any change to ai_test's 13 untracked entries or under `.claude/`/`Assets/Tests/` after T0.5/T0.6 → GATE: PASS, unrecoverable.
HOLDS_UP: Increment 9 end-to-end; hook-injection.test.sh hardcoded (banker, wolf-mailbox, wolf-response, wolfnotes-agents hooks untested); external-skills.json superpowers-only; 0.90 internal-duplicate risk correctly described (no guard key exists); `plugin-zero-components` at skill-validator.ts:477-488; checks 2/4/6 verbatim-correct; Increment 7 re-target genuinely additive (`recompile_status` exactly once, IA:460 under the authoring heading); destructive-gate has the strongest margin (0 hits for undo/irreversible/without saving; SK:432 teaches `--non-interactive` + `--yes` together); T6.0 sequencing closed; §4 rows 3a/3b/4 fix the prior ordering (Judge disagrees on the 3a→5 routing — see synthesis); sandbox gap real (0 hits); T2.2 S5's `git checkout <stash> -- <path>` works for tracked snapshot-dirty files.

## Orchestrator synthesis (round 2)
VERDICT: REJECTED — one grounded FAIL (Critic) with six executed fatal flaws in revision-2's new machinery; Judge's six essentials overlap five of them. The revision closed every round-1 fatal (three reviewers agree) and Increment 9 is verified correct against origin/main.
DISAGREEMENT: Critic "rows 3a/3b/4 fix the ordering cleanly" vs Judge E4 "row 3a GO TO ROW 5 skips 3b". The Judge quotes the text and IA:162-164; the Critic did not examine the routing target. Judge stands: OPEN → fix (3a routes to 3b, then 5).
FIX_LIST round 2 (all MUST unless marked):
R2-1 Hook project detection: derive the Unity root from (a) stdin `.cwd` walk-up, (b) for Write/Edit `dirname(file_path)` walk-up, (c) for Bash, any segment whose first word is `unity` (or ends in `/unity`) counts as Unity context regardless of cwd; never `$PWD`. Note `CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=1` in settings.
R2-2 Scenario tagging: replace the env var with a flag file `${STATE}/scenario.current` written by the runner before each dispatch and removed after; the hook reads it; every record also stores stdin `session_id`. Delete G18's "discard and re-run" rule in favour of "records with an empty scenario tag during a known scenario window are attributed by session_id".
R2-3 `destructive-cli`: split the command on `;`, `&&`, `||`, `|`; a segment matches only if its first word (after optional `VAR=val` prefixes) is `unity`/`*/unity` AND it carries the destructive subcommand or flag; `pkill -f Unity`/`killall Unity` match standalone; `grep`/`echo`/`git` can never match. Metric 4's tripwire requires `pattern=="destructive-cli"` AND `subcommand=="close"` fields, not a regex over the command text.
R2-4 Patterns non-exclusive: record `patterns:[…]` (all matches) plus `pattern` (first); `create_script` inside live-mutation also sets `cs_write:true`.
R2-5 Registration during development (fixes Critic H1 + F5): add `unity-ops/tests/stage.sh` that assembles `/tmp/unity-ops-stage/{.claude-plugin,hooks,skills/<selected>}`; every scenario run is a runner-spawned headless session `cd ~/Dev/Unity/ai_test && claude -p --plugin-dir /tmp/unity-ops-stage --output-format json "<prompt>"` (baseline: stage WITHOUT the skill under test; result force-fed: stage WITH it and the prompt prefixed "read unity-ops:<skill> first"; result natural: stage WITH it, bare prompt — the real router decides and `unity-cli` competes for real from `~/.claude/skills`). The runner sets `UNITY_TEST_TIMEOUT=600 UNITY_BUILD_TIMEOUT=1800 UNITY_RUN_TIMEOUT=600` in that child's env (env DOES propagate to a process the runner spawns). T1.3 must observe, in such a session: the hook fires; it fires for a tool call made by a subagent inside that session (prompt it to use the Agent tool); `additionalContext` reaches the model (ask the model to quote any guardrail context it received). If any of the three fails, DESIGN §4A falls back to alternative C — say so.
R2-6 Falsifiable gates: the recording template carries `VERDICT_RED: __` / `TRIGGERED: __` placeholder lines the grader fills with YES/NO; gates grep `^VERDICT_RED: YES$` exactly; briefs live in `tests/briefs/<skill>.md`, never inside transcripts; the trigger signal is mechanical — the `claude -p --output-format json` transcript contains a Skill tool use whose input names `unity-ops:<skill>` (grep the JSON for `"unity-ops:<skill>"` inside a tool_use, not the whole file).
R2-7 Testbed gate: snapshot hashes ALL untracked files (`git ls-files --others --exclude-standard | xargs shasum`) and tracked-dirty files; gate computes both `now−before` and `before−now` and hash deltas; T0.5/T0.6 re-snapshot (rebase) so new subtrees are hashed, not excluded; recovery = `git update-ref refs/unity-ops/snapshot <stash-obj>` (keeps it from GC) PLUS `tar czf ${STATE}/ai_test-untracked.tgz` of the untracked set; the snapshot file's placeholder is asserted replaced (`grep -c '<paste' == 0`).
R2-8 Decision table: row 3a routes to row 3b (headless check via `unity list --project-path`), and only then to row 5; fix the copied `references/decision-table.md`, §3.1's evidence row, and PLAN:1384 ("no second exception" → the headless exception IS documented at IA:162-164; the sandbox exception is the one that is not).
R2-9 Correct false absence claims: `vcs uvcs locks|changesets|review` documented (SK:257,263,280; PT:175-220) — absent: the new verb family + `vcs doctor`; `plugin install plastic` documented (SK:286; PT:259) — absent: `plugin changelog`. Fix DESIGN:88, :392-393, :739, §3.6 table; fix anchors SK:26→32, SK:17→15, SK:16/38→36.
R2-10 T0.4 degradation: if no project-path field is observed on `data.instances[]`, row 1 must NOT fire on machine-wide Safe Mode; degrade to ASK.
R2-11 CUT-aware Increment 9: Expecteds computed from `ls unity-ops/skills | wc -l`; CHANGELOG lists surviving skills; Increment 3's CUT row: cli-contract cannot be cut (it carries the dependency stop) — if 3/3 comply it ships as a reference skill with the stop only.
R2-12 T9.5 S1: non-interactive form with an Expected (`claude -p --plugin-dir … --output-format json "List the skills available to you whose names start with unity-ops:"`); no `.unity` write.
R2-13 Restatement measured mechanically: `tests/restatement-audit.sh` — 8-word shingles of each SKILL.md body line vs the installed skill tree; share = matched lines / body lines ≤ 0.25; becomes conformance check 7; the `restates` markers stay as documentation.
R2-14 T9.2 S2 `grep -c … || echo` double line (C13 class) — fix. T6.2 S3 timeout RED criterion: grep the "Proposed command line" section for `--timeout|UNITY_[A-Z]+_TIMEOUT`; RED = absent.
R2-15 SHOULD: G19 wording (`-h` under Options; a Global Options block follows); "eleven-row" → twelve; "seven gates" → six + setup step; CHANGELOG date placeholder assertion; preflight budget check asserts numerator/denominator ratio; README:147; delete the leftover self-correction at PLAN:1632; T0.5 "at least `unity-pipeline`; record what else lands"; `test -f` vs `test -x` consistent (use `bash` invocation, `test -f`); add an "Errata 2026-09-14" block at the top of R1 (nested `--help` intermittent; vcs/plugin rows) and fix R1b's `BRT:414`.

---
---

# ROUND 3 — re-review of revision 3 (2026-09-14)

Revision 3: DESIGN.md 937 lines, PLAN.md 4444 lines / 57 tasks; hook rewritten on stdin `cwd`/`file_path`/unity-fronted segments; flag-file scenario tagging; staged headless `claude -p --plugin-dir` harness; falsifiable `VERDICT_RED`/`TRIGGERED` gates; snapshot hashes all untracked files; `restatement-audit.sh` as check 7; decision table 3a→3b→5; vcs/plugin absence claims corrected; Increment 9 CUT-aware. The `claude` CLI is logged out on this machine (`claude auth status` → loggedIn:false; verified by the orchestrator) so the harness was designed against the documented shape, not observed; the plan treats that as PRECONDITION_FAILED.

## Judge (round 3) — PASS_WITH_CONCERNS
PRIOR_FIXES: R2-1, R2-2, R2-7, R2-8, R2-9, R2-12, R2-14, E2, E4, E5 RESOLVED; R2-3, R2-4, R2-5, R2-6, R2-10, R2-11, R2-13, R2-15, E1, E3, E6 PARTIAL. Citations: 235 anchor tokens / 68 sites spot-checked, 0 dangling. Increment 9 re-verified against origin/main. CLI premises re-verified live (status exit 6 STATUS_NO_INSTANCES; pipeline list exit 0; --project-path exit 2).
ESSENTIAL: J1 T1.3 Observation 3 creates the probe project's ProjectVersion.txt AFTER the session that writes into it → the hook's Write branch finds no project → "NO GUARDRAIL CONTEXT RECEIVED" → the plan's own veto fires deterministically (PLAN:1771-1774). J2 `trig()` requires the namespaced skill name; real transcripts carry the bare name ~40-50% of the time for the same skills (sampled ~/.claude/projects) → false TRIGGER-FAIL. J3 Check 7 defeated by line length: 900 verbatim words reflowed at 6 words/line → share 0.12 PASS; at 20 words/line → 1.00 FAIL; the installed SKILL.md scores 0.59 against itself; §2A row counts and check 7 use different denominators. J4 The `close` tripwire still ships as the revision-2 regex in the Task 9.5 issue body (PLAN:4275), and the `tr ';|&'` splitter is quote-blind: `git commit -m "gate unity close; unity close is now blocked"` → destructive-cli/close; `pattern` is first-added not most-severe (`unity job status && unity close` → pattern unity-invocation). J5 T1.1 Step 3 runs stage.sh before hooks.json/guard/plugin.json exist (created in T1.2/T2.0) → cp fails. J6 DESIGN §3.1 RED still predicts a hand-edit "without the Safe-Mode check", but installed SK:186 (the paragraph right after SK:184) mandates `unity pipeline list` first; only the sandbox half is absent — PLAN:2008 says so, DESIGN §3.1 does not. J7 Logged-out CLI: the harness's sole precondition is failing today, absent from DESIGN entirely (0 hits), no risk row, no connection-audit row, no degraded path (alternative C still needs `claude -p`). J8 `Packages/packages-lock.json` (tracked) is rewritten when the Editor resolves the package → T0.3's gate fails until T0.5's re-snapshot; T0.2's whitelist covers only manifest.json and doesn't hash it.
NON-ESSENTIAL: `unity close` takes a required `<project>` positional and "exits without saving" is in the ROOT `unity --help` (line 54); run_scenario has no trap/lock/atomic write; Observation 3 not mechanically falsifiable (needs a nonce); Observation 2 can't attribute to the subagent (assert distinct session_id); matcher `Bash|Write|Edit` vs script handling MultiEdit|NotebookEdit; unwritable state dir leaks stderr; jq-absent = silent empty log; case (f) Expected omits the stdout advisory line; T6.2 awk concatenates reps; non-ASCII C-quoted paths dropped silently from hashes; stale labels `NEW ENTRIES` (PLAN:705, :3511); T9.2 S2 `echo "$S" | wc -l` = 1 on empty; share printed `{:.2f}` can show 0.25 for a failing 0.254; `claude auth status --format json` → unknown option (dead clause); `ALC:` prefix missing from the legend; three soft off-by-one range heads; no CI gate reads the CHANGELOG; T8.1 runs `unity command eval` (arbitrary C#) unauthorized by G5/G6 and `time unity job wait` foreground; PLAN:3850 hard-codes "7 scenarios + 6 baselines + 7 results"; PLAN:4191 "seven CI gates"; PLAN:3850 `test -x` narrative; DESIGN:811/:848 still describe the killed `$PWD` guard; PLAN:4324/:4360 say row 1 "degrades to the machine-wide summary" (contradicting R2-10); R5 unchanged (still recommends UNITY_OPS_SCENARIO, still ships the `$PWD` skeleton, rows 12/18 bare URLs) while DESIGN:45 calls it "verified".

## Scorer (round 3) — CONDITIONAL_PASS, composite 9.0
R2-1..R2-13 RESOLVED (R2-5 PARTIAL: T1.3 Part B unobservable while logged out); R2-14 PARTIAL (T0.2 S1 `grep -c … || echo` double line survives); R2-15 PARTIAL (PLAN:3850 `test -x` narrative). EXECUTED: snapshot/gate a–f byte-identical to the plan's table; hook a–i + 10 extra cases exact incl. 10 ms latency; restatement 2/8→0.25 PASS, 3/8→0.38 FAIL, full tree 11 files/36468 shingles; stage.sh all four cases; trig() 1 vs 0; seven conformance checks exact. Live: claude 2.1.247 flags accurate; auth logged out; ai_test 33/13/0 asmdef; origin/main 16 plugins/4-key/[3.4.0]/35 skills/0 root unity-*. NO items: mid-batch OAuth expiry unhandled; Node 20 in CI vs local Node v26.5.1 unexamined; T0.2 S1 double line; T9.1 `test -x` narrative false (hook fires via `bash` regardless of exec bit).

## Critic (round 3) — FAIL
PRIOR: F1, F2, F4, H1, H2, H3, M2, M3, M4 CLOSED; F3 trigger half OPEN; F5 measurement OPEN; F6 PARTIALLY OPEN; M1 partially; M5 OPEN; WB items closed except quoted/heredoc forms.
EXECUTED_ATTACKS (hook): `cat unity-ops/PLAN.md` no record ✓; `./unity close` record ✓; `sudo unity close`, `sudo -E …`, `xargs unity close`, `nohup unity close &`, `timeout 60 unity close`, `bash -c 'unity close'`, `command unity close`, `exec unity close`, `eval 'unity close'`, `$(unity close)`, backticks → NO record (wrapper hole); heredoc body line `unity close` → record (false positive); quoted segments split inside quotes: `echo "a; unity close /tmp/p"`, `git commit -m "note; unity close proj"`, `grep -n 'a|unity projects clean --yes' f` → destructive-cli; T5.3 S2's own heredoc writing `| unity close <proj> |` → destructive-cli/close → metric 4 tripwire; `--project-path` value never used for project detection; symlinked project recorded as symlink path; `.prefab.meta`/`.unity.meta` writes → no record; MultiEdit .cs → cs-write ✓; fails open under all degradations ✓. Gate: chmod-only PASS (miss), empty dir PASS (miss), writes in gitignored Library/ PASS (miss), Assets/Logs/ file excluded by is_log. Restatement: robust to one-word changes and scans fences/tables ✓; 12 short verbatim lines → 0.21 PASS; all-table-row skill → 0.00 PASS (short-line floor). stage.sh: `references/` symlink copied as symlink, stray guard doesn't follow; duplicate args nest; `../hooks` accepted; name≠dirname unchecked. trig(): different unity-ops skill → 0 ✓; unity-cli use → 0 ✓; prose → 0 ✓; subagent nested in tool_result → 0; NDJSON input prints two lines. `input.skill` shape verified on a real transcript — bare 74 vs namespaced 73.
FATAL (ESSENTIAL): C1 trig() coin-flip on bare vs namespaced names. C2 check 7 blind to short lines (all-table-row verbatim skill → 0.00 PASS). C3 destructive-cli tripwire fires on T5.3's own heredoc deliverable and on quoted segments; PLAN:1892/DESIGN:549 assert impossible. C4 wrapper-shaped hole (sudo/nohup/timeout/xargs/command/exec/eval/bash -c/$(…)) voids the promotion path.
HIDDEN (ESSENTIAL): H1 the `-p --output-format json` payload shape was never observed (auth failure emits no assistant envelope); H2 subagent Skill use invisible in `json` (`--forward-subagent-text` only with stream-json); H3 no `--permission-mode`/`--allowedTools` passed to child sessions; user settings defaultMode auto with a 3-entry allow list → a denied Bash probe reads as RED; H4 after T0.5 the natural contest has three competitors (`~/.claude/skills/unity-cli` + `ai_test/.claude/skills/unity-cli` + `unity-pipeline`), never reconciled. NON-ESSENTIAL: `refs/unity-ops/snapshot` + tarball never deleted and not in T0.7's mutation log; gate blind to Library/ and Logs/-containing paths; stage.sh symlink/name checks; `claude plugin eval` exists per docs and is never mentioned.
MISSING (ESSENTIAL): stale labels `NEW ENTRIES` (PLAN:705, :3511) and `SNAPSHOT-DIRTY FILES ALTERED` (PLAN:2809) — no script prints them; R2-13 half-applied (`restates ≤N` Expecteds at PLAN:2904/3122/3377/3574 and refactor text at :3165/:3422/:3618 with no producing command); no trap/lock on the flag file, no absent-check before dispatch, session_id attribution bootstrapped from tagged records; no wait between T0.6's `unity test` and its final re-snapshot (.meta files appear on import). NON-ESSENTIAL: PLAN:3850 `test -x`; PLAN:4185/4188 "(1 hook + 6 skills)", :4189 "six trigger evals"; no dispatch checks stage.sh's rc.
WILL_BREAK: bare-name trigger → false TRIGGER-FAIL + unwarranted issue; delegated skill use invisible; T5.3 heredoc → tripwire; overlapping/backgrounded run_scenario → earlier run untagged and unrecoverable; hung dispatch → stale flag tags all real work forever; .meta files after T0.6's re-snapshot → every later gate FAIL; wrapped destructive calls → no record/no deny; name≠dirname → false TRIGGER-FAIL; denied Bash in child → RED for the wrong reason.
HOLDS_UP: hook correct on every claimed case and many more; gate genuinely fixes F4 both directions; restatement robust where it can see; trig() discriminates correctly on the namespaced form; stage.sh rebuilds/rejects/refuses outside /tmp; checks 1–6 exact; all six descriptions pass 2 and 4; claude --help quotes accurate; --plugin-dir loads skills and hooks and namespaces plugin:skill (docs); Increment 9 verified; PRECONDITION_FAILED correctly firing today.

## Orchestrator synthesis (round 3)
VERDICT: REJECTED for execution (one grounded FAIL with executed defects), but the defect class has narrowed to instruments and harness details — no architectural item remains. Judge and Scorer both PASS with concerns; the Critic's four fatals are each one function's worth of change.
DISAGREEMENTS: none of substance — the Judge's J2/J3/J4 are the Critic's C1/C2/C3.
FIX_LIST round 3 (all MUST unless SHOULD):
R3-1 Trigger signal: primary = a hook record with `tool:"Skill"` whose `input.skill` ∈ {`<name>`, `unity-ops:<name>`} and whose session_id is the run's (add `Skill` to the matcher `Bash|Write|Edit|MultiEdit|NotebookEdit|Skill` and a `skill-invocation` pattern that records `skill`); secondary = `trig()` on the transcript accepting bare OR namespaced (`select(. == $S or . == ($S|sub("^[^:]+:";"")))`), printing exactly one line for both array and NDJSON input. This also makes subagent skill use visible (hooks fire in subagents) regardless of `--output-format`.
R3-2 Restatement audit: word-coverage metric — shingle length k = min(6, words) with a floor of 4; a line is restated when ≥60% of its words are covered by matched shingles; lines with <4 normalized words and pure-structure lines (`|---|`, fences, headings) are excluded from numerator and denominator; the installed SKILL.md must score ≥0.95 against itself and an all-verbatim `| Thought | Reality |` fixture must FAIL; §2A row counts relabeled "design estimate; check 7 is the measurement"; share printed `{:.3f}`, verdict compares numerically.
R3-3 Segment splitter: quote-aware via python3 `shlex` (posix, punctuation_chars=';|&'); heredoc bodies stripped (`<<`/`<<-` marker through terminator); leading wrappers stripped before the first-word test: `sudo [-E|-u x]`, `nohup`, `timeout N`, `time`, `command`, `exec`, `env [VAR=val…]`, `xargs`; `bash -c '…'`, `sh -c`, `eval '…'`, `$(…)`, backticks recurse one level into the quoted string; `pattern` = most severe by a fixed severity order; `subcommand` from the destructive segment; `--project-path <p>` in a unity segment sets `project` when cwd/file walk-ups fail; `.meta` companions of `.unity|.prefab|.asset` count as serialized-asset-write. Metric 4's tripwire = `tool=="Bash" and pattern=="destructive-cli" and subcommand=="close"`; the Task 9.5 issue body uses the field form. Tests: every Critic attack line above with the expected outcome stated, including T5.3's heredoc → no record.
R3-4 Child-session permissions: pass an explicit allow list to every `claude -p` dispatch (check `claude --help` for `--allowedTools`/`--permission-mode` syntax): Read, Grep, Glob, Write, Edit, Skill, Agent, `Bash(. *)`, `Bash(unity status*)`, `Bash(unity list*)`, `Bash(unity command*)`, `Bash(unity pipeline list*)`, `Bash(unity test*)`, `Bash(unity build*)`, `Bash(git status*)`, `Bash(git diff*)`; everything else denied non-interactively; the recording template gains `PERMISSION_DENIALS: n` extracted mechanically from the transcript, and a rep with ≥1 denial of a unity probe is INCONCLUSIVE, not RED.
R3-5 Payload shape probe: new T1.3 Step 0b — after auth, run `claude -p --output-format json --verbose "reply with the single word ok" > tests/transcripts/shape-probe.json`, record the top-level shape; `trig()` and every transcript parser are asserted against that probe file; if the shape has no `assistant`/`tool_use` envelopes, switch dispatches to `--output-format stream-json --verbose` (documented in `claude --help` as the format that forwards subagent text) and adapt the parser. Mid-batch: run_scenario re-checks `claude auth status` before each rep; auth failure → PRECONDITION_FAILED for that rep.
R3-6 Competitors after T0.5: remove `ai_test/.claude/skills/unity-cli` (byte-identical duplicate of the user-level copy — assert with `diff -r` first), keep `unity-pipeline`; add both facts to T0.7's mutation table; state the two competitors (`unity-cli`, `unity-pipeline`) wherever the contest is described.
R3-7 Flag file: `trap 'rm -f "$FLAG"; rmdir "$LOCK" 2>/dev/null' EXIT INT TERM`; `mkdir "$STATE/scenario.lock" || { echo "another scenario run is in progress"; exit 2; }`; assert the flag is absent before dispatch; primary attribution = the child's `session_id` captured from the transcript and written to `tests/transcripts/<run>.session`; metrics filter on the set of those ids (tag stays a convenience).
R3-8 T0.6: after `unity test`, poll until `git status --porcelain Assets/Tests | wc -l` is stable for 15 s before the final re-snapshot. T0.2: whitelist `Packages/packages-lock.json` and hash `Packages/manifest.json`.
R3-9 T1.3: Observation 3 creates the probe project BEFORE dispatch, and the advisory carries a per-run nonce the model must quote; Observation 2 asserts the new record's session_id differs from the parent's (or uses a command only the subagent was told to run); T1.1 Step 3's stage smoke test moves to the end of T1.2 (after hooks.json, guard and plugin.json exist).
R3-10 DESIGN: §3.1 RED re-scoped to the sandbox half (SK:186 mandates the Safe-Mode check); risk row + connection-audit row for the logged-out CLI with the degraded path (an interactive `claude --plugin-dir` session driven by hand, slower but valid); §4A/:811/:848 stale `$PWD` guard text removed; `unity close <project>` positional and root-help source (line 54) corrected; `ALC:` added to the legend; DESIGN:45 "verified" → "documented; rows 12/18 observed in T1.3"; R5 gets an errata block (UNITY_OPS_SCENARIO withdrawn; `$PWD` skeleton superseded by PLAN T1.2; rows 12/18 pending observation).
R3-11 Stale text sweep in PLAN: `NEW ENTRIES` (:705, :3511) → `ADDED (status lines absent from snapshot)`; `SNAPSHOT-DIRTY FILES ALTERED` (:2809) → `ALTERED (hash changed)`; remove `restates ≤N` Expecteds (:2904, :3122, :3377, :3574) and refactor text (:3165, :3422, :3618); :3850 `test -x` and artifact counts; :4185/:4188/:4189 "(1 hook + 6 skills)"/"six trigger evals" → `$N`; :4191 "seven"; :4324/:4360 row-1 wording; T0.2 S1 `grep -c … || echo`; case (f) Expected stdout line; T6.2 awk per-rep; T9.2 S2 `ls | wc -l`; `claude auth status --format json` dead clause; T8.1: background `job wait`, authorize the sleep-only `eval` explicitly in G5 for ai_test; three range-head off-by-ones.
R3-12 SHOULD: gate records file mode in the hash line; `assert len(hashes)==len(untracked|tracked_dirty)`; stage.sh `cp -RL`, reject duplicate/non-basename args, check `name:` == dirname; dispatch blocks check stage.sh rc; Increment 9 cleanup task removes `refs/unity-ops/snapshot` and the tarball (and T0.7 lists them); note `claude plugin eval` as an alternative harness if present on 2.1.247 (`claude plugin --help`); note CI Node 20 vs local Node 26; unwritable state dir append gets `2>/dev/null` inside the script.

---

## Revision 4 applied (2026-09-14) — reviser's summary
DESIGN.md 1004 lines; PLAN.md 5383 lines / 58 tasks. R3-1…R3-12 all applied. Hook classifier moved to a single python3 pass (shlex, posix, punctuation_chars=';|&'): heredoc bodies stripped, `$(…)`/backticks/`bash -c`/`eval` recursed one level, wrappers peeled (sudo/nohup/timeout/time/command/exec/env/xargs/setsid/nice/stdbuf), most-severe `pattern`, `subcommand` from the producing segment, `--project-path` fallback, `.meta` companions, `Skill` tool → `skill-invocation` record with `skill` field; matcher `Bash|Write|Edit|MultiEdit|NotebookEdit|Skill`. TRIGGERED primary = hook record join on the run's session_id (bare or namespaced); `trig()` secondary with `sub("^[^:]+:";"")`, one line for array/NDJSON. Restatement audit = word coverage (k=max(4,min(6,words)), ≥60% coverage, structure lines excluded, 3-decimal share). Dispatch = `run_scenario.sh` with mkdir lock + pid, trap on EXIT/INT/TERM/HUP, dead-owner recovery, per-rep `claude auth status`, session_id captured to `<transcript>.session`; `--permission-mode dontAsk --allowedTools <15 entries>` on every dispatch (flags verified against `claude --help` 2.1.247; bogus mode exits 1). Shape probe Step 0b with stream-json fallback. T0.5 removes the duplicate project-local `unity-cli` after `diff -r`. T0.6 polls `.meta` stability 15 s before the final re-snapshot; T0.2 whitelists packages-lock.json and hashes manifest.json. T1.3 Observation 3 creates the probe project before dispatch with a nonce; Observation 2 asserts a distinct session_id; stage smoke test moved to T1.2 S5. DESIGN §3.1 RED re-scoped to the sandbox half; risks 20/21/22 (logged-out CLI + degraded interactive path; inherited permission mode; unobserved payload shape); `unity close <project>` + root-help line 54; legend `ALC:`/`gh-`; R5 errata table (T1.1 S6 prepends it). Stale-text sweep complete (0 hits for NEW ENTRIES / restates ≤N / (1 hook + 6 skills) / test -x). Gate: REMODED counter, `len(hashes)==len(want)` assert with core.quotePath=false, `is_log` limited to root Logs/, `git restore --staged` after a ref restore. stage.sh: cp -RL, rejects duplicate/path-shaped/name≠dirname. Task 9.6 cleans the snapshot ref, tarball and lock; T0.7 lists them. Executed suites: hook 40/40 (incl. all Critic attacks; unwritable state → 0 stderr bytes; Unity-payload latency 48–127 ms stated honestly, non-Unity 10 ms), restatement 4/4 (installed SKILL.md vs itself → 1.000 FAIL; all-verbatim table → 1.000 FAIL; 12 short verbatim → 1.000 FAIL; 1-of-8 → 0.125 PASS), trig 10/10, gate 11/11 (incl. chmod, Assets/Logs/, C-quoted path assert, packages-lock whitelist), run_scenario 6/6 (overlap rc=2, SIGTERM/SIGKILL cleanup, stale flag refused, real run today → PRECONDITION_FAILED rc=3), stage.sh 6/6. Not applied: `claude -p` never dispatched (logged out, by rule); `Library/`/empty-dir gate blindness left as documented; `gh-SK:443` kept as an explicit GitHub anchor. NOTE for reviewers: `grep` on this machine is a ugrep alias — a mid-pattern `^` inside `[...]` behaves differently; use `grep -F` for literal checks.

---
---

# ROUND 4 — re-review of revision 4 (2026-09-14)

## Judge (round 4) — PASS_WITH_CONCERNS
PRIOR_FIXES: R3-1…R3-5, R3-7, R3-8, R3-9, R3-12, J1–J6, J8 RESOLVED; R3-6 resolved as written (see E3); R3-10/R3-11/J7 PARTIAL. 44 spot-checked DESIGN citations: 0 dangling, 0 contradicting. Increment 9 re-verified against origin/main. COHERENCE: 9/15 cross-references agree; disagreements are stale counts/revision numbers (PLAN:47/:632/:4734 "revision 2/3"; DESIGN:930 "hook lands at increment 2" vs row 1; "nine patterns"/"other six" vs ten; PLAN:4693 "seven gates"; PLAN:914/940 "hook needs jq"; hard-coded `six` at PLAN:4808/5027/5031/5376).
ESSENTIAL: E1 the pre-filter `case "$INPUT" in *unity*|*.cs*|…` matches the whole stdin JSON — cwd under a repo named "Unity Claude Skills" costs 125 ms/call with 0 records; content "a community opportunity" 180 ms; `main.css` edit 216 ms; DESIGN:564/:912 and PLAN:2504 claim the cost is paid only on Unity-shaped payloads. E2 `command -v python3` is a false positive on macOS (the `/usr/bin/python3` xcselect stub exists without Command Line Tools) → guard must be `python3 -c 'pass'`; no risk row for interpreter availability/provenance; advertised latency depends on the user's python (pyenv shim 0.10 s vs system 0.03 s). E3 T0.5's `rm -rf ai_test/.claude/skills/unity-cli` is a mutation class no approval covers (authority cited as "[R3-6]"); `unity skill refresh` / repeat `--local` can recreate it after the last re-snapshot → later gates blame a scenario. E4 the degraded path for a logged-out CLI ("drive an interactive session by hand") is not executable by the mandated agentic worker, voids the allow-list envelope, and leaves PERMISSION_DENIALS/VERDICT_RED ungradable without a JSON transcript.
NON-ESSENTIAL: DESIGN:9 legend `auth-licensing-cloud.md` (file is `auth-license-cloud.md`); PLAN legend lacks ALC/gh-; `data.count:0` cited to IA:337 (not there; live-true); BRT:43 reuse scoped to `run --command`; BRT:310 "disabled by default" is build-only; IA:348/IA:252 off-by-one; `claude auth status` exits 1 when logged out (bare calls under set -e); pager documented but never ported (installed CHANGELOG:34); `--max-turns` acceptance unverifiable by the stated method.

## Scorer (round 4) — CONDITIONAL_PASS, composite 9.3
R3-1…R3-4, R3-6, R3-8…R3-12 RESOLVED; R3-5 PARTIAL (shape probe unobserved; logged out); R3-7 PARTIAL. EXECUTED: hook 40/40 exact (10 ms non-Unity, 128 ms Unity-shaped); run_scenario R1,R2,R4,R5,R6 exact — **R3 (SIGTERM) does NOT reproduce**: bash defers the trap while blocked on the foreground child, so flag/lock persist until the child exits; snapshot/gate 10/10; restatement F1/F4 byte-exact, F2/F3 qualitatively exact; stage.sh 6/6; trig/trig_hook/den exact; conformance 1–7 exact; live CLI/git/gh/origin/main all exact. Weakest: Testability 8.75 (the SIGTERM claim).

## Critic (round 4) — FAIL
PRIOR: C1 CLOSED; C2 PARTIALLY (3-word verbatim lines and verbatim headings score 0/0 PASS; 6-verbatim+5-novel per line → PASS, 7+4 → FAIL — boundary gameable by ~45% padding); C3 CLOSED (T5.3 heredoc, quoted segments, git commit -m all 0 records); C4 PARTIALLY (twelve named wrappers closed; control flow open — F5); H1 PARTIALLY (harness still unobserved); H2 CLOSED by mechanism; H3 PARTIALLY (F3); H4 CLOSED; stale-label items CLOSED; M "(1 hook + 6 skills)" class OPEN in PR title/body and issue bodies; all round-3 WILL_BREAK closed except the two partials.
EXECUTED_ATTACKS: 33 round-3 cases byte-identical. NEW no-record (10): `for p in a b; do unity close $p; done`, `while read p; do unity close "$p"; done < list`, `if unity status; then unity close /p; fi`, `case … ) unity close /p;; esac`, `{ unity close /p; }`, `(unity close /p)`, `find . -exec unity close {} \;`, `python3 -c "os.system('unity close /p')"`, `u='unity'; $u close /p`, alias. NEW correct (8): pipes, extra spaces, env prefix, `&`, `set -e;`, `||`, `cd … && unity close .`, relative file_path. Arguable FPs: Edit under `Library/*.unity`; NotebookEdit .unity. trig_hook join breakers: trailing space / uppercase skill names; non-Unity Skill calls produce no record (metric 7 blind to non-Unity competitors). Malformed input: all fail open. Latency: 50 KB command 186 ms; 500 KB 2.7 s; 1 MB 9.4 s; 3 MB >60 s (super-linear). Gate: `.gitignore` edit, tracked deletion, file→dir, symlink retarget all FAIL correctly; packages-lock asymmetry as documented; empty dir and Library/ blind (documented). Snapshot: dangling symlink → assert fires AFTER write_text; rc never checked → snapshot written under-guarded, GATE: PASS after retargeting the unguarded symlink (F1). Restatement: fenced code blocks count as prose; non-Latin text tokenizes to nothing (excluded silently). run_scenario: lock dir with no pid file → permanently stuck `(pid ?)`; transcript with no session_id → exit 0 + empty `.session`; one empty `.session` turns SCEN_SESSIONS into a regex with an empty alternative → every metric returns 0; `skill: null` → metric 7's sub() errors. den(): scores 0 on "Permission to use Bash … has been denied" and "This command requires approval" (dontAsk never emits the interactive wording). stage.sh: uppercase dir accepted; missing SKILL.md → raw awk error rc 2. Expected sweep: PLAN:2612 `grep -c ': OBSERVED$'` → 1 not 4 on a fully successful run (alignment padding) → abandons the hook (F2); `grep -c pat f1 f2` prints prefixed lines (cosmetic).
FATAL: F1 snapshot regression guard inert (assert after write; rc unchecked at all three sites). F2 increment 1's success path scores 1/4 and triggers the alternative-C fallback. F3 allow list omits `unity --version`, `unity skill install --list`, the `export` envelope and the grep half of pipes (all required by the plan's own Expecteds); `den()` cannot see a dontAsk denial. F4 PLAN's preflight RED rubric (running `pipeline list` = NOT-RED; CUT requires "both" false negatives) contradicts DESIGN:208 (sandbox question alone) in the direction that cuts the skill four Chains depend on. F5 shell control flow (`for/while/if/case/{ }/( )`) bypasses the first-word rule; `find -exec`/runtime execution invisible.
HIDDEN: harness unobserved (logged out); Observation 2 assumes subagent hook payloads carry a different session_id (documented payload has `agent_id`/`agent_type`, unrecorded); §10 alternative C unreachable while logged out; check 7 counts lines not rows, fenced command lines count; pre-filter makes metric 7 blind to non-Unity competitors; `Bash(. *)` grants sourcing any file.
MISSING: snapshot rc assertion; lock-without-pid recovery; no-session_id handling; agent_id/agent_type + null-skill guard; log size cap/rotation/truncation; latency bound on the Unity path; trig_hook normalization; stale counts that ship as literal text (PR title "six verification gates", body "plus six skills"/"revision 2", issue "the six DX metrics" (seven), metrics.md index six, "seven gates" :4693, "nine patterns", "seven-row table" :3442 vs eight, DESIGN:188-189 undefined pattern names, PLAN:3704/:3753 `close --help` attribution, DESIGN:1 "three open questions", DESIGN:976 "8 skills", DESIGN:644 "Three", DESIGN:980 §9(c)).
HOLDS_UP: classifier fixed on everything round 3 broke; gate strongest instrument (four new attacks caught); trig() exact; restatement fixture 1 to the shingle; run_scenario core; stage.sh hermetic; metrics.md jq all execute; conformance 1–6 exact; PLAN:940 dependency grep live = 1; all `claude --help` quotes accurate; zero unclosed fences/placeholders.

## Orchestrator synthesis (round 4) — and the stopping decision
Judge PASS_WITH_CONCERNS, Scorer CONDITIONAL_PASS (9.3), Critic FAIL on five executed local defects. Severity trend across rounds: architectural → mechanism → instrument → polish/edge. Jeremy asked whether this is churn; the orchestrator's answer: rounds 1–3 were not (each changed what would ship); round 4 is where the curve flattens. DECISION: stop the review loop. One final pass (revision 5) applies the Judge's four essentials, the Scorer's SIGTERM fix, the Critic's F1–F5 and the essential MISSING items (C-M1…C-M5) plus the stale-text list — with executed tests pasted — and NO further review round. Remaining blockers are Jeremy's: `claude` CLI login; decision Q6 (delete the duplicate project-local `unity-cli` copy in ai_test, or keep both competitors and record which fires).
